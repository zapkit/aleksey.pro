<section id="proof" data-animate class="relative border-y border-neutral-100/5">
  <div class="container py-16">
    <div class="mx-auto mb-8 max-w-lg text-center">
      <h2 class="text-3xl font-bold md:text-4xl">Real performance results</h2>
      <p class="mt-3 text-neutral-400">
        Websites are optimized not only visually but technically — for speed, usability and search
        visibility.
      </p>
    </div>
    <div class="flex flex-wrap items-center justify-center gap-12">
      <div class="flex flex-col items-center">
        <div class="relative h-28 w-28">
          <svg viewBox="0 0 120 120" class="-rotate-90">
            <circle cx="60" cy="60" r="54" fill="none" stroke="#262626" stroke-width="5"></circle>
            <circle
              cx="60"
              cy="60"
              r="54"
              fill="none"
              stroke="oklch(72.3% 0.219 149.579)"
              stroke-width="5"
              stroke-linecap="round"
              stroke-dasharray="339"
              stroke-dashoffset="339"
              class="animate-speedUp"></circle>
          </svg>
          <div class="absolute inset-0 grid place-items-center text-xl font-semibold">
            <span class="animate-fadeOut">10s</span>
            <span class="animate-fadeIn absolute">1.5s</span>
          </div>
        </div>
        <p class="mt-3 text-center text-neutral-400">real load time improvement</p>
      </div>

      <div class="flex flex-col items-center">
        <div class="relative h-32 w-20 overflow-hidden rounded-xl border border-neutral-600 p-2">
          <div
            class="from-primary to-primary/80 animate-uiFlow absolute inset-2 rounded-xl bg-linear-to-b"
          >
          </div>
          <div class="bg-neborder-neutral-100/60 absolute top-6 right-4 left-4 h-2 rounded"></div>
          <div class="bg-neborder-neutral-100/60 absolute top-10 left-4 h-2 w-1/2 rounded"></div>
        </div>
        <p class="mt-3 text-center text-neutral-400">mobile-first interface</p>
      </div>

      <div class="flex flex-col items-center">
        <div class="relative w-56 rounded-xl border border-neutral-700 bg-neutral-900 p-3">
          <div class="mb-3 h-6 w-full rounded bg-neutral-800"></div>
          <div class="space-y-2 text-left text-xs">
            <div class="opacity-40">
              <div class="h-2 w-32 rounded bg-neutral-600"></div>
              <div class="mt-1 h-2 w-48 rounded bg-neutral-700"></div>
            </div>
            <div class="relative">
              <div class="bg-primary h-2 w-36 animate-pulse rounded"></div>
              <div class="mt-1 h-2 w-52 rounded bg-neutral-500"></div>
            </div>
            <div class="opacity-40">
              <div class="h-2 w-40 rounded bg-neutral-600"></div>
              <div class="mt-1 h-2 w-44 rounded bg-neutral-700"></div>
            </div>
          </div>
        </div>
        <p class="mt-3 text-center text-neutral-400">business website ranked in Google results</p>
      </div>
    </div>
  </div>
</section>

<script is:inline>
  const section = document.querySelector("#proof");

  if (section) {
    const observer = new IntersectionObserver(
      (entries, obs) => {
        entries.forEach((entry) => {
          if (!entry.isIntersecting) return;

          section.classList.add("is-visible");
          obs.unobserve(section);
        });
      },
      {
        threshold: 0.25,
      },
    );

    observer.observe(section);
  }
</script>
